__pycache__/
*.pyc
.cache/
*.parquet
paper_state_*.json
fx_state_*.json
# SQLite book stores (source of truth) + WAL/shared-memory sidecars
paper_books.db
fx_books.db
*.db-wal
*.db-shm
# …but DO track the CI-managed state (see .github/workflows/paper-trade.yml)
!state/paper_state_*.json
!state/fx_state_*.json
!state/paper_books.db
!state/fx_books.db
paper.log
equity_curve*.csv
sweep_*.csv
reports/
# Run manifests + experiment ledger (F17): per-run reproducibility records.
# Match at the repo root too (a backtest run outside CI uses the repo root as its
# state dir, so `manifests/` and the ledger land here, not under state/).
state/manifests/
manifests/
experiment_ledger.jsonl
# Derived ML artifacts (trained models + reports) — uploaded by the FX Action.
trading_algo/forex/models/*.json
fx_ml_report.md
*_ml_report.md
.pytest_cache/
.venv/
venv/
*.egg-info/

# Obsidian vault: keep notes + app/appearance config, ignore per-machine state
obsidian/.obsidian/workspace*.json
obsidian/.obsidian/cache
obsidian/.obsidian/*.bak

# Superpowers brainstorming companion (local mockups)
.superpowers/

# mutation-testing scratch
mutants/
.mutmut-cache
